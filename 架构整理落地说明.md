# 架构整理落地说明

## 文档用途

这份文档不是“重构完成报告”，而是给下一任继续推进时使用的交接说明。

目标只有三个：

- 说明目前已经推进到了哪里
- 说明这一阶段刻意没有动什么
- 说明下一步最应该从哪个中等粒度工作包继续往前推

请继续保持“小步推进、边改边验证、避免一次性大拆”的工作方式，但不要再回到“一次只补一个文案点或一个局部字符串”的推进粒度。

---

## 一页速览

### 目前已经立起来的边界

- `ApplicationPreferences`：应用级默认偏好
- `ConversionJobDefinition`：当前任务 / 冻结任务的定义
- `WatchProfile`：监听模式配置
- `PreviewToolState` / `PreviewSessionState`：预览工具状态与会话上下文
- `ConversionPlan` / `ConversionRuleSummary` / `ConversionDiagnosticsSummary`：执行前的轻量规划、共享规则摘要、共享诊断摘要
- `RuntimeStatusSummary` / `CompletionSummaryModel`：手动与监听共享的运行中 / 完成后摘要语义基础
- `WatchRuntimeStatusSummary`：监听模式正式的运行中摘要对象，不再只靠局部字符串拼装
- `ConversionSummaryCoordinator` / `ConversionSummaryFlowResult` / `ConversionSummaryDialogRequest`：手动与监听共享的“完成后摘要 -> 历史档案 -> 弹窗请求”协调层
- `HistoryState`：历史列表、历史持久化读取、历史卡片展示文本、清空动作的独立子域
- `NotificationState`：失败日志入口、待展示完成弹窗请求的独立子域
- `TaskAnalysisState`：右侧任务分析面板的警告、规则摘要、格式推荐、体积预估与免责声明子域

### 目前已经落地的关键方向

- 监听模式不再长期直接吃主界面的瞬时状态，而是优先依赖冻结快照
- AI 抠图已经从批量转换 / 预设语义里逐步抽离，回归预览工具语义
- `ConversionPlanningService` 已经开始承担“规则显式化”的职责
- `AI 适用性 / 跳过 AI / 监听模式附加规则` 已经开始共享同一份规则摘要，而不是继续在多个出口各自手拼
- `高压缩 / GIF 转 PDF 大帧 / PDF 锁定跳过 / 体积预估免责声明` 已经开始共享同一层 diagnostics 语义，而不是继续散落在 `ConversionViewModel`
- `ConversionTextPresenter` 已开始承担“完成摘要弹窗 / 历史卡片 / 监听状态文本”的共享格式化职责
- `ConversionSummaryCoordinator` 已开始承担“完成后摘要生命周期协调”的职责，manual/watch 不再各自手拼历史项与弹窗文案
- `HistoryState` / `NotificationState` 已经把“完成后遗留状态”从 `MainWindowViewModel` 主体里拆出，主 ViewModel 不再直接持有历史集合与失败日志路径
- `TaskAnalysisState` 已经把“任务分析 + 风险提示 + 格式推荐 + 体积预估”从 `MainWindowViewModel` 的扁平字段里拆出，`RefreshConversionInsights()` 现在改为“生成分析快照 -> 更新子域状态”
- `MainWindow.axaml.cs` 不再订阅手工 completion event，而是改为消费 `NotificationState` 上的待展示弹窗请求
- 右侧预览区已经有了“本次任务分析”这类明确的规则出口
- `PreviewPanel.axaml` 的警告 / 任务分析 / 格式推荐 / 体积预估四块现在已经直接绑定 `TaskAnalysisState`，不再继续吃主 ViewModel 的这组平铺属性
- 预览区已经不再直接复用任务级 `CurrentFile`，而是回到“当前选中文件”语义
- 体积预估已经补上 AI / 多页 PDF / 多帧 GIF 的免责声明
- 透明背景被压平成背景填充的规则，已经开始从 `ViewModel` 临时判断收口到 `ConversionPlan`

### 当前仍然故意没有动的高风险区

- 没有直接大拆 `MainWindowViewModel`
- 没有直接大拆 `ImagePreviewWindow`
- 没有全面移除 `AppServices`
- 没有重写整个转换执行主链
- 没有一次性删除旧模型上的全部历史字段

一句话总结当前状态：

> 现在还不是“已经整理完”，而是“已经把正确的推进边界、共享摘要语义和下一段真正该收口的主线打出来了”。

---

## 到目前为止已经推进了什么

下面只保留真正对后续接手有帮助的主线，不再展开成大段流水账。

### 已经完成的阶段性收口

- 状态边界已经立起来：`ApplicationPreferences`、`ConversionJobDefinition`、`WatchProfile`、`PreviewToolState`、`PreviewSessionState`、`ConversionPlan` 已经不是概念草图，而是实际开始承接职责。
- 旧设置兼容层已经立起来：`Services/AppSettingsStateMapper.cs` 已经把旧设置对象与新状态模型之间的映射收口，后续迁移不需要再回到“大而全”的旧对象上直接扩字段。
- 监听模式已经从“主界面瞬时状态的附属触发器”往“长期运行任务”推进：启用监听时固化 `ConversionJobDefinition` 快照，监听执行优先吃冻结规则，也支持显式“保存当前参数为监听配置”。
- AI 抠图已经开始回归预览工具语义：相关状态从批量转换 / 预设语义里逐步抽离，避免继续和主批处理管线混用。
- 轻量规划层已经开始接管解释性规则：`ConversionPlanningService` 已承接 AI 是否生效、哪些输入跳过 AI、GIF/PDF 展开、预计输出项、工作项、体积预估免责声明等说明性信息。
- 透明被压平为背景填充的规则，已经不再只是 `ViewModel` 里的局部判断，而是开始收口到 `ConversionPlan`，并被转换前预警、右侧任务分析、当前选中文件告警共同消费。
- 手动转换已经用 `RuntimeStatusSummary` 和 `CompletionSummaryModel` 收拢“运行中状态 / 完成后摘要 / 历史摘要”的共享语义。

### 我这一轮新增推进

- 已新增 `Models/TaskAnalysisSnapshot.cs` 与 `ViewModels/Main/TaskAnalysisState.cs`，把右侧任务分析面板对应的展示状态正式收口成独立 feature state。
- `RefreshConversionInsights()` 已从“直接改一串 `MainWindowViewModel` 扁平字段”改成“构建 `TaskAnalysisSnapshot` -> 交给 `TaskAnalysisState.Apply()` 更新”，`MainWindowViewModel` 不再直接持有 `ActiveWarnings`、`ConversionPlanHighlights`、格式推荐、体积预估、免责声明这组 sidebar 状态。
- 原本散在 `RefreshConversionPlanInsights()` / `RefreshActiveWarnings()` 里的任务分析刷新链，本轮已经统一折叠回 `TaskAnalysisSnapshot` 构建路径；这一步的意义不是只换地方，而是让右侧分析面板开始拥有单一的数据入口。
- `Views/Controls/PreviewPanel.axaml` 本轮已把警告、任务分析、格式推荐、体积预估四块直接改为绑定 `TaskAnalysisState`，不再继续通过主 ViewModel 的这组扁平属性中转。
- `HistoryState`、`NotificationState`、`TaskAnalysisState` 现在已经在 `MainWindowViewModel` 里并排成为同一层 feature state，主 ViewModel 的“壳层化”又往前推进了一步。
- 本轮顺手删掉了围绕 task analysis/sidebar 的旧扁平出口与旧集合事件钩子，避免形成“新 state 已有、旧字段仍同步”的第三条双轨。
- 本轮仍然没有新增 UI 文案键，因此没有扩散 10 语种维护面；后续若再新增文案键，仍要同步简中、繁中、英语、日语、韩语、德语、法语、意大利语、俄语、阿拉伯语这 10 种语言。
- 已补回归测试：`Tests/ImvixPro.Tests/ImageConversionTests.cs` 现在额外覆盖 `TaskAnalysisState` 的 apply / 清空行为，确保这次不是“把字段搬去新类里”而是可验证的状态收口。

### 当前仍未解掉的核心堵点

- 右侧任务分析虽然已经独立成 `TaskAnalysisState`，但“当前选中文件预览 + GIF/PDF/PSD 预览渲染 + 预览区交互可见性”这一整片 preview 子域仍然直接挂在 `MainWindowViewModel` 上，`SelectedImage` 变更后的预览刷新链仍然很重。
- `MainWindow.axaml.cs` 已经从 completion event 退到消费 `NotificationState`，但文件选择、预览打开、Tray、窗口宿主逻辑仍然集中在同一个窗口代码后置文件里，没有进一步形成更薄的 shell 协调层。
- `MainWindowViewModel` 和 `ImagePreviewWindow` 仍然是两个大宿主，边界虽然更清楚了，但结构风险还在。
- `AppServices` 仍然是运行期跨层入口，依赖关系还不够透明。
- `AppSettings` / `ConversionOptions` / `ConversionPreset` 仍然有兼容层噪音，不能再继续横向长字段。

---

## 当前刻意没有做的事

为了控制风险，下面这些事情仍然是有意识地先不硬拆：

- 不直接大拆 `MainWindowViewModel` partial 体系
- 不直接大拆 `ImagePreviewWindow` 工具宿主
- 不直接重写转换执行主链
- 不直接把 `AppServices` 一次性拿掉
- 不为了“看起来干净”一次性删除旧模型所有历史字段
- 不引入新的测试工程或大批一次性验证脚本

原因不变：

> 如果在规则层还没进一步收稳的时候，同时叠加结构拆分、依赖迁移、执行器重写，风险会急剧上升。

---

## 下一任最建议从哪里继续推

### 先说结论

下一任不要再回头做 `history + notification` 或 `task analysis sidebar` 这一包的补丁了，这两包现在都已经正式收口了，继续在这里小修只会重新掉回“边角优化”。

更适合下一任接手的是一个比本轮更重、而且应该明确做成“子域抽离”的工作包：

> **PreviewSelectionState + MainWindowShellCoordinator 收口包**

目标不再是继续收右侧任务分析，而是把“当前选中文件预览、GIF/PDF/PSD 预览渲染状态、预览区文件级配置可见性、窗口代码后置里的宿主协调”这一整片一起拿下来，直接开始削掉 `MainWindowViewModel` 与 `MainWindow.axaml.cs` 的下一整片重量。

### 下一任最推荐的中等偏重工作包

建议下一任至少同时完成下面五件事：

1. 把 `SelectedPreview`、PDF/GIF/PSD 预览加载状态、预览可见性判定、预览相关提示文案所依赖的文件级状态，从 `MainWindowViewModel` 收口到独立 `PreviewSelectionState` 或等价子域，不要再让主 ViewModel 继续直接维护这整块“当前选中文件预览”逻辑。
2. 把 `OnSelectedImageChanged()` 里现在仍然串在一起的“清理旧预览 -> 恢复 GIF/PDF 选择 -> 触发 GIF/PDF/PSD 预览加载 -> 刷新一堆可见性属性”这条重链，改造成“生成预览上下文 -> 交给独立 preview state / coordinator 更新”的收口链。
3. 让 `PreviewPanel.axaml` 的“文件预览相关区域”开始直接绑定新 preview state，而不是继续通过主 ViewModel 拿一长串 `Is*Visible`、`*HintText`、`*IndicatorText` 之类的扁平属性。
4. 同时把 `MainWindow.axaml.cs` 里文件选择、预览窗口打开、Tray、窗口可见性、待展示对话框消费这几类宿主逻辑，收口到更薄的 `MainWindowShellCoordinator` 或等价壳层，不要继续让 code-behind 成为第二个平铺式总控点。
5. 做完后必须把旧的 preview 扁平字段、旧刷新出口和旧窗口分支一起删掉，不要留下“新 preview state 已有，但主 ViewModel 和 code-behind 里老逻辑还在同步”的三轨。

### 为什么我建议下一任把范围继续加大

- 这轮已经把“右侧任务分析 sidebar”收住了，再回头继续抠这一块，收益会明显下降。
- 现在真正拖慢推进速度的，已经变成“选中文件预览状态 + preview 渲染链 + shell 宿主逻辑”这一整片 still-heavy 的区域；它依然要求 `MainWindowViewModel` 和 `MainWindow.axaml.cs` 共同维护很多时序分支。
- 只要下一任把 `preview selection + shell coordination` 这一整片子域抽出来，后面无论是继续拆 `MainWindowViewModel`，还是整理 `PreviewPanel` / `ImagePreviewWindow` 的职责边界，都会一下子轻很多。
- 这个建议虽然比本轮更重，但仍然没有越线到“重写执行主链”或“直接大拆预览窗口”，风险仍是可控的，而且比继续做小补丁更接近真正提速。

### 建议的实际落地顺序

第一步：

- 盘点 `MainWindowViewModel` 与 `MainWindow.axaml.cs` 里仍然属于“当前选中文件预览 / 宿主协调”子域的全部内容：`SelectedPreview`、GIF/PDF/PSD 预览加载、预览相关 `Is*Visible` / `*HintText` / `*IndicatorText`、预览窗口打开、Tray、窗口可见性与待展示请求消费。

第二步：

- 先立一个独立 `PreviewSelectionState` / preview coordinator 入口，让 `SelectedImage` 切换后的预览上下文、加载状态与文件级显示状态先收成结构化状态，再由 UI 消费，而不是继续直接往主 ViewModel 和 code-behind 上铺一堆布尔值与分支。

第三步：

- 把 GIF/PDF/PSD 预览刷新链、文件级提示文案、预览区可见性判断一起迁过去，并让 `PreviewPanel.axaml` 开始直接绑定新 preview state，而不是继续向主 ViewModel 要一堆扁平属性。

第四步：

- 把 `MainWindow.axaml.cs` 里围绕文件选择、预览打开、Tray、可见性消费的宿主分支进一步收口掉，同时删掉 `MainWindowViewModel` / code-behind 里围绕 preview 的旧字段与旧入口，避免形成新的三轨。

第五步：

- 完成后必须做 `build + test + 启动 + 测试残留清理`。

### 如果下一任还有余力

- 可以顺手开始把 `PreviewPanel.axaml` 里“文件预览”和“历史/故障/任务分析”视觉上进一步分组，为后续真正拆 `Preview / Task Analysis / History Diagnostics` 三块区域做准备。
- 可以顺手开始盘点 `MainWindow.axaml.cs` 还剩下哪些分支适合进一步收口为 shell 层，尤其是和窗口生命周期、托盘、对话框消费相关的路径。
- 但下一轮仍然不建议直接大拆 `ImagePreviewWindow` 或直接全面迁移 `AppServices`，除非前面的 preview state / shell coordinator 已经都收稳并且验证稳定。

---

## 下一任暂时不建议马上做的事

- 不建议直接全面拆 `MainWindowViewModel`
- 不建议直接全面迁移 `AppServices`
- 不建议一边重写执行主链、一边重做预览窗口、一边收规则层
- 不建议为了表面整洁一次性删掉大量旧字段

当前更好的策略是：

> 先用一个中等偏重工作包，把 `preview selection + shell coordination` 这块状态子域一起拿下来，再考虑更大的结构动作。

---

## 一个实用判断标准

如果你新增或调整一条规则时，出现了下面任一情况：

- 需要同时改很多个 `ViewModel` 条件分支
- 需要同时补很多个 UI 入口说明
- 需要猜“手动转换”和“监听模式”是否表现一致
- 需要在预警、摘要、提示、历史里重复写相同逻辑

那基本说明这条规则还没有真正进入 `ConversionPlan / WatchProfile / SummaryCoordinator / Policy` 这一层。

这时正确动作通常不是继续堆分支，而是先收口，然后顺手删掉旧出口。

---

## 建议继续保持的工作方式

- 不要再按“一个极小点一轮”推进，改成“一个中等范围规则包一轮”。
- 先做“边界更清楚、规则出口更统一”，再做“大结构更漂亮”。
- 不新增无谓的测试垃圾、临时脚本和一次性验证残留。
- 收尾时清理生成产物，但不要误删真实测试源码和夹具。
- 每次改完至少保证 `build` 通过、主程序可以正常启动。

---

## 最近一轮收尾记录

### 本轮额外完成的事

- 已新增 `TaskAnalysisSnapshot` 与 `TaskAnalysisState`，让右侧任务分析面板正式拥有独立的 feature state 宿主。
- 已让 `RefreshConversionInsights()` 改成“构建分析快照 -> 更新 `TaskAnalysisState`”，不再继续直接改主 ViewModel 上一整组 sidebar 扁平字段。
- 已删掉 `MainWindowViewModel` 上围绕 task analysis/sidebar 的旧字段、旧 `Has*` 计算属性与旧集合事件钩子，避免形成新旧双轨。
- 已让 `PreviewPanel.axaml` 直接绑定 `TaskAnalysisState` 的警告、任务分析、格式推荐、体积预估四块区域，减少主 ViewModel 的中转职责。
- 已让 `HistoryState`、`NotificationState`、`TaskAnalysisState` 在 `MainWindowViewModel` 里并排成为同一层 feature state，进一步明确主 ViewModel 正在向壳层收缩。
- 已补回归测试，确认新的 task analysis 子域 apply / 清空行为稳定。
- 本轮没有新增 UI 文案键，也没有新增临时验证工程。
- 本轮收尾已清理 `Tests/ImvixPro.Tests/bin` 与 `Tests/ImvixPro.Tests/obj` 这类测试运行生成残留，没有删除真实测试源码与夹具。

### 本轮验证方式

```powershell
$env:DOTNET_CLI_HOME=(Resolve-Path '.dotnet-home').Path
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE='1'
$env:DOTNET_NOLOGO='1'
dotnet build "Imvix Pro.csproj" -c Debug --no-restore
dotnet test "Tests\ImvixPro.Tests\ImvixPro.Tests.csproj" -c Debug --no-restore
$exe = (Resolve-Path 'bin\Debug\net10.0\Imvix Pro.exe').Path
$process = Start-Process -FilePath $exe -PassThru
Start-Sleep -Seconds 4
Get-Process -Id $process.Id | Select-Object Id, ProcessName, MainWindowTitle, Responding, Path
Stop-Process -Id $process.Id
Remove-Item "Tests\ImvixPro.Tests\bin","Tests\ImvixPro.Tests\obj" -Recurse -Force
```

### 本轮验证结果

- `dotnet build` 通过，`0 error`
- 仍有既存的 CA1416 平台分析警告
- 无新增编译错误
- `dotnet test` 退出码为 `0`
- 测试总数现为 `39`，本轮新增 task analysis state 相关回归用例已通过
- 启动验证通过，主窗口标题正常，`Responding = True`
- 测试运行残留已清理，未误删真实测试源码

---

## 给下一任的最后一句话

这份工作到现在，已经不适合再按“一个摘要文案点一轮”或“一个 history / notification 小入口一轮”去推进了。

summary flow 的对象层、history/notification 的结果态、task analysis sidebar 这三块现在都已经基本收住了，下一任请把火力放到“preview selection + shell coordination 子域收口”这一整包上，至少做到下面三件事：

- 多拿掉一整片 `MainWindowViewModel` 上的选中文件预览 / 预览加载 / 文件级可见性扁平状态
- 少留一处 `PreviewPanel / MainWindowViewModel / MainWindow.axaml.cs` 三处并存的旧三轨
- 改完还能稳定 `build + test + 启动`

只要做到这一点，这个项目就会继续明显从“渐进止血”转向“可持续维护”，而且推进速度会比现在更像“开始持续拿掉主宿主与窗口壳层的双重负担”，而不再只是“继续补 sidebar 或摘要边角”。
