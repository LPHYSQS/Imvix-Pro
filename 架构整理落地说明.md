# 架构整理落地说明

## 文档用途

这份文档不是“重构完成报告”，而是给下一任继续推进时使用的交接说明。

目标只有三个：

- 说明目前已经推进到了哪里
- 说明这一阶段刻意没有动什么
- 说明下一步最应该从哪个中等粒度工作包继续往前推

请继续保持“小步推进、边改边验证、避免一次性大拆”的工作方式，但不要再回到“一次只补一个文案点或一个局部字符串”的推进粒度。

---

## 一页速览

### 目前已经立起来的边界

- `ApplicationPreferences`：应用级默认偏好
- `ConversionJobDefinition`：当前任务 / 冻结任务的定义
- `WatchProfile`：监听模式配置
- `PreviewToolState` / `PreviewSessionState`：预览工具状态与会话上下文
- `PreviewSelectionSnapshot` / `PreviewSelectionState`：当前选中文件预览、PDF/GIF/PSD 文件级可见性与预览面板显示状态的独立子域
- `ConversionPlan` / `ConversionRuleSummary` / `ConversionDiagnosticsSummary`：执行前的轻量规划、共享规则摘要、共享诊断摘要
- `RuntimeStatusSummary` / `CompletionSummaryModel`：手动与监听共享的运行中 / 完成后摘要语义基础
- `WatchRuntimeStatusSummary`：监听模式正式的运行中摘要对象，不再只靠局部字符串拼装
- `ConversionSummaryCoordinator` / `ConversionSummaryFlowResult` / `ConversionSummaryDialogRequest`：手动与监听共享的“完成后摘要 -> 历史档案 -> 弹窗请求”协调层
- `HistoryState`：历史列表、历史持久化读取、历史卡片展示文本、清空动作的独立子域
- `NotificationState`：失败日志入口、待展示完成弹窗请求的独立子域
- `TaskAnalysisState`：右侧任务分析面板的警告、规则摘要、格式推荐、体积预估与免责声明子域

### 目前已经落地的关键方向

- 监听模式不再长期直接吃主界面的瞬时状态，而是优先依赖冻结快照
- AI 抠图已经从批量转换 / 预设语义里逐步抽离，回归预览工具语义
- `ConversionPlanningService` 已经开始承担“规则显式化”的职责
- `AI 适用性 / 跳过 AI / 监听模式附加规则` 已经开始共享同一份规则摘要，而不是继续在多个出口各自手拼
- `高压缩 / GIF 转 PDF 大帧 / PDF 锁定跳过 / 体积预估免责声明` 已经开始共享同一层 diagnostics 语义，而不是继续散落在 `ConversionViewModel`
- `ConversionTextPresenter` 已开始承担“完成摘要弹窗 / 历史卡片 / 监听状态文本”的共享格式化职责
- `ConversionSummaryCoordinator` 已开始承担“完成后摘要生命周期协调”的职责，manual/watch 不再各自手拼历史项与弹窗文案
- `HistoryState` / `NotificationState` 已经把“完成后遗留状态”从 `MainWindowViewModel` 主体里拆出，主 ViewModel 不再直接持有历史集合与失败日志路径
- `TaskAnalysisState` 已经把“任务分析 + 风险提示 + 格式推荐 + 体积预估”从 `MainWindowViewModel` 的扁平字段里拆出，`RefreshConversionInsights()` 现在改为“生成分析快照 -> 更新子域状态”
- `MainWindow.axaml.cs` 不再订阅手工 completion event，而是改为消费 `NotificationState` 上的待展示弹窗请求
- `MainWindowShellCoordinator` 已经接手 completion dialog 消费、第二实例提示、Tray hide/restore/exit、BringToFront，以及预览窗口 / 文件详情窗口 / PDF 解锁弹窗的宿主协调入口
- 右侧预览区已经有了“本次任务分析”这类明确的规则出口
- `PreviewPanel.axaml` 的警告 / 任务分析 / 格式推荐 / 体积预估四块现在已经直接绑定 `TaskAnalysisState`，不再继续吃主 ViewModel 的这组平铺属性
- 预览区已经不再直接复用任务级 `CurrentFile`，而是回到“当前选中文件”语义
- `PreviewSelectionState` 已经开始承接 preview 子域的展示状态，`PreviewPanel.axaml` 的文件预览相关区域不再继续直接绑定一长串主 ViewModel 扁平 preview 属性
- `PreviewRenderCoordinator` 已经开始承接 `SelectedImage` 切换后的 preview render 时序，`MainWindowViewModel` 不再直接手拼“清理旧预览 -> 恢复 per-file 选择 -> 触发 GIF/PDF/PSD preview load”这一整段分支
- 体积预估已经补上 AI / 多页 PDF / 多帧 GIF 的免责声明
- 透明背景被压平成背景填充的规则，已经开始从 `ViewModel` 临时判断收口到 `ConversionPlan`

### 当前仍然故意没有动的高风险区

- 没有直接大拆 `MainWindowViewModel`
- 没有直接大拆 `ImagePreviewWindow`
- 没有全面移除 `AppServices`
- 没有重写整个转换执行主链
- 没有一次性删除旧模型上的全部历史字段

一句话总结当前状态：

> 现在还不是“已经整理完”，而是“已经把正确的推进边界、共享摘要语义和下一段真正该收口的主线打出来了”。

---

## 到目前为止已经推进了什么

下面只保留真正对后续接手有帮助的主线，不再展开成大段流水账。

### 已经完成的阶段性收口

- 状态边界已经立起来：`ApplicationPreferences`、`ConversionJobDefinition`、`WatchProfile`、`PreviewToolState`、`PreviewSessionState`、`ConversionPlan` 已经不是概念草图，而是实际开始承接职责。
- 旧设置兼容层已经立起来：`Services/AppSettingsStateMapper.cs` 已经把旧设置对象与新状态模型之间的映射收口，后续迁移不需要再回到“大而全”的旧对象上直接扩字段。
- 监听模式已经从“主界面瞬时状态的附属触发器”往“长期运行任务”推进：启用监听时固化 `ConversionJobDefinition` 快照，监听执行优先吃冻结规则，也支持显式“保存当前参数为监听配置”。
- AI 抠图已经开始回归预览工具语义：相关状态从批量转换 / 预设语义里逐步抽离，避免继续和主批处理管线混用。
- 轻量规划层已经开始接管解释性规则：`ConversionPlanningService` 已承接 AI 是否生效、哪些输入跳过 AI、GIF/PDF 展开、预计输出项、工作项、体积预估免责声明等说明性信息。
- 透明被压平为背景填充的规则，已经不再只是 `ViewModel` 里的局部判断，而是开始收口到 `ConversionPlan`，并被转换前预警、右侧任务分析、当前选中文件告警共同消费。
- 手动转换已经用 `RuntimeStatusSummary` 和 `CompletionSummaryModel` 收拢“运行中状态 / 完成后摘要 / 历史摘要”的共享语义。

### 我这一轮新增推进

- `PreviewRenderCoordinator` 这轮把第一大块里剩余的 GIF 会话收口补完了：`GIF handling mode change`、`specific-frame playback toggle`、`RestoreGifSpecificFrameSelection()` 的延迟回填现在都改为先走 coordinator，再决定是复用已加载帧、启动播放、还是回落到重新加载 preview。
- `PreviewViewModel` 不再继续自己判断“GIF handling mode 改了以后要不要重新加载 preview”；它现在只负责准备静态占位预览与底层 GIF handle 装配，真正的 reload / request-id 分流已经收进 coordinator。
- `GifSpecificFrameViewModel` 不再自己判断“指定帧播放是直接启动、还是先加载 GIF preview”，也不再在 restore 延迟回填里直接套用预览；它现在只保留低层 index / playback helper，时序决策已经从 partial 里拔掉。
- `MainWindowHostController` 已经落地，把 tray icon 初始化与更新、窗口位置 restore/save、startup path import、导入/输出/监听目录选择器这些宿主杂务从 `MainWindow.axaml.cs` 里抽走，主窗口 code-behind 明显瘦了一层。
- `ImagePreviewWindow` 已经落下第一刀真实 tool controller：`PreviewBarcodeToolController` 开始承接条码识别工具的入口与结果整形，预览窗口不再只停留在“准备盘点 controller”的阶段。
- 本轮仍然没有新增 UI 文案键，因此没有扩散 10 语种维护面；后续若再新增文案键，仍要同步简中、繁中、英语、日语、韩语、德语、法语、意大利语、俄语、阿拉伯语这 10 种语言。
- 已补回归测试：`Tests/ImvixPro.Tests/ImageConversionTests.cs` 这轮又新增钉住了 `GIF handling mode -> coordinator`、`specific-frame playback toggle -> coordinator`、`restore delayed apply -> coordinator` 三条路径；加上上一轮的 `PDF 页切换 / GIF trim range / GIF specific frame`，第一大块相关入口现在都有回归钉子。

### 当前仍未解掉的核心堵点

- `Preview/Host Consolidation 半场收口包` 到这一轮为止已经完成，后面不要再回头补这半场，除非是明确回归。
- `MainWindowViewModel` 和 `ImagePreviewWindow` 仍然是两个大宿主，边界虽然更清楚了，但结构风险还在。
- `AppServices` 仍然是运行期跨层入口，依赖关系还不够透明。
- `AppSettings` / `ConversionOptions` / `ConversionPreset` 仍然有兼容层噪音，不能再继续横向长字段。
- `ImagePreviewWindow` 虽然已经有了第一刀 `PreviewBarcodeToolController`，但距离“2 个以上真实 tool controller 的宿主”还差最后一段，需要下一任直接把第二大块收完。

---

## 当前刻意没有做的事

为了控制风险，下面这些事情仍然是有意识地先不硬拆：

- 不直接大拆 `MainWindowViewModel` partial 体系
- 不直接大拆 `ImagePreviewWindow` 工具宿主
- 不直接重写转换执行主链
- 不直接把 `AppServices` 一次性拿掉
- 不为了“看起来干净”一次性删除旧模型所有历史字段
- 不引入新的测试工程或大批一次性验证脚本

原因不变：

> 如果在规则层还没进一步收稳的时候，同时叠加结构拆分、依赖迁移、执行器重写，风险会急剧上升。

---

## 下一任最建议从哪里继续推

### 先说结论

第一大块 `Preview/Host Consolidation 半场收口包` 已经在这一轮收完；下一任不要再回头补 `history + notification`、`task analysis sidebar`，也不要再围着 `PreviewSelectionState`、`PreviewRenderCoordinator`、`MainWindowHostController` 做零碎修补了。

从这份交接说明开始，剩余工作不再是“两大块里的第一块和第二块”，而是只剩 **最后一大块**：`Composition/Tool Host Finalization 终局收口包`。把这一块做完，这一阶段的架构整理就该直接收官。

### 两大块的拆分方式

#### 第一大块：已经完成的半场收口包

> **Preview/Host Consolidation 半场收口包**

这一块已经完成，不再作为下一任任务重新打开。它本轮收掉的内容是：

1. `GIF handling mode change / specific-frame playback toggle / restore delayed apply` 已经全部收进 `PreviewRenderCoordinator`。
2. `PreviewViewModel`、`GifSpecificFrameViewModel` 里那批“coordinator 已经能管，但旧 reload / playback 判断还在”的旧分支已经删掉一轮。
3. `MainWindow.axaml.cs` 上的 tray / placement / startup / picker 宿主杂务已经进入 `MainWindowHostController`。
4. `ImagePreviewWindow` 已经落下第一刀 `PreviewBarcodeToolController`，不再是零 controller 状态。
5. 本轮涉及到的旧 helper / 旧转调已经同步清掉，没有把第一大块继续留成三轨并存。
6. `build + test + 启动 + 测试残留清理` 已做完，且本轮没有新增 UI 文案键。

#### 第二大块：下一任现在唯一剩余的大块

> **Composition/Tool Host Finalization 终局收口包**

这一块现在就是下一任要直接吃完的最后半场，不要再往回拆碎：

1. 把 `ImagePreviewWindow` 从“继续增长的大宿主”正式推进到“至少有 2 个以上真实 tool controller 的宿主”，让第一刀不再停留在样板。
2. 把 `MainWindowViewModel` 开始从 partial 大宿主推进到更明确的 feature state / coordinator 结构，优先拆 preview/watch/shell 相关交界面，而不是再横向加字段。
3. 开始压缩 `AppServices` 在 main-window / preview path 上的运行期直连，把最常走的入口收进更清晰的装配边界，为后续 composition root 定型。
4. 把 `AppSettings` / `ConversionOptions` / `ConversionPreset` 的字段归属再收紧一轮，至少把“当前任务 / 默认偏好 / 预览工具 / 监听任务”的边界从兼容噪音里再往前推进一步。
5. 做完整的 `build + test + 启动` 收尾，并把这两大块推进后的新边界重新写回交接说明，作为这一轮架构整理阶段的收官文档。

### 为什么现在必须这样加重

- 第一大块已经完成，再回头补它只会重新掉回“边角整洁”，不会继续降低主风险。
- 现在真正剩下的风险已经集中到 `ImagePreviewWindow` 的 tool-host 终局、`MainWindowViewModel` 的 feature state / coordinator 化、以及 `AppServices` 与旧配置模型边界的最后一轮收紧。
- 如果下一任还把这最后半场拆回 2 到 3 个中包继续拖，最后又会回到“还要很多轮”的旧节奏；这里要在文档层面直接阻断。
- 所以下一任的任务要继续保持可控重，但目标已经变成“直接收最后一大块”，而不是再把它拆成一串小修补。

### 这两大块的顺序要求

第一大块已经完成，下一任直接进入第二大块即可。

- 不要重新打开 `Preview/Host Consolidation 半场收口包`
- 直接完成 `Composition/Tool Host Finalization 终局收口包`

不要把剩余这一大块重新拆散成多个中包，也不要再把它回退成零碎修补。

### 如果下一任还有余力

- 可以在完成第二大块硬目标后，再顺手做一点更细的 `AppServices` 替换点盘点或模型字段归属文档补全。
- 但不要在第二大块还没整体收完时，又重新发散到执行主链重写或无边界的大迁移。

---

## 下一任暂时不建议马上做的事

- 不建议直接全面拆 `MainWindowViewModel`
- 不建议直接全面迁移 `AppServices`
- 不建议一边重写执行主链、一边重做预览窗口、一边收规则层
- 不建议为了表面整洁一次性删掉大量旧字段

当前更好的策略是：

> 第一大块已经收完；现在剩余工作只剩最后一大块，下一任应直接把它完整收掉，而不是再拆回多个小包。

---

## 一个实用判断标准

如果你新增或调整一条规则时，出现了下面任一情况：

- 需要同时改很多个 `ViewModel` 条件分支
- 需要同时补很多个 UI 入口说明
- 需要猜“手动转换”和“监听模式”是否表现一致
- 需要在预警、摘要、提示、历史里重复写相同逻辑

那基本说明这条规则还没有真正进入 `ConversionPlan / WatchProfile / SummaryCoordinator / Policy` 这一层。

这时正确动作通常不是继续堆分支，而是先收口，然后顺手删掉旧出口。

---

## 建议继续保持的工作方式

- 不要再按“一个极小点一轮”推进，改成“一个中等范围规则包一轮”。
- 先做“边界更清楚、规则出口更统一”，再做“大结构更漂亮”。
- 不新增无谓的测试垃圾、临时脚本和一次性验证残留。
- 收尾时清理生成产物，但不要误删真实测试源码和夹具。
- 每次改完至少保证 `build` 通过、主程序可以正常启动。

---

## 最近一轮收尾记录

### 本轮额外完成的事

- 已把 `GIF handling mode change`、`specific-frame playback toggle`、`RestoreGifSpecificFrameSelection()` 延迟回填收进 `PreviewRenderCoordinator`，第一大块剩余的 GIF preview session 分支已经闭环。
- 已把 `PreviewViewModel` 从“直接决定 GIF preview 是否 reload”退回到底层 prepare / load helper，把 `GifSpecificFrameViewModel` 从“直接决定 playback/load/apply”退回到底层 specific-frame helper。
- 已新增 `MainWindowHostController`，把 tray icon、window placement、startup path、folder picker 这些宿主级逻辑从 `MainWindow.axaml.cs` 抽离。
- 已为 `ImagePreviewWindow` 落下第一刀真实 tool controller：`PreviewBarcodeToolController`。
- 已补 3 条回归测试，额外钉住 `GIF handling mode -> coordinator`、`specific-frame playback toggle -> coordinator`、`restore delayed apply -> coordinator` 这三条路径。
- 本轮没有新增 UI 文案键，也没有新增临时验证工程。
- 本轮收尾已检查测试项目下没有遗留 `bin` / `obj` / `TestResults` 测试残留，也没有删除真实测试源码与夹具。

### 本轮验证方式

```powershell
$env:DOTNET_CLI_HOME=(Resolve-Path '.dotnet-home').Path
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE='1'
$env:DOTNET_NOLOGO='1'
$env:APPDATA=(Resolve-Path '.appdata').Path
$env:LOCALAPPDATA=(Resolve-Path '.localappdata').Path
$env:USERPROFILE=(Resolve-Path '.profile').Path
$env:HOME=$env:USERPROFILE
$env:NUGET_PACKAGES=(Resolve-Path '.nuget').Path
dotnet build "Imvix Pro.csproj" -c Debug --no-restore
dotnet test "Tests\ImvixPro.Tests\ImvixPro.Tests.csproj" -c Debug --no-restore
$exe = (Resolve-Path 'bin\Debug\net10.0\Imvix Pro.exe').Path
$process = Start-Process -FilePath $exe -PassThru
Start-Sleep -Seconds 5
Get-Process -Id $process.Id | Select-Object Id, ProcessName, MainWindowTitle, Responding, Path
Stop-Process -Id $process.Id
```

### 本轮验证结果

- `dotnet build` 通过，`0 error`
- 仍有既存的 `9` 条 `CA1416` 平台分析警告；另外由于当前环境离线，仍会看到 `NU1900` 的漏洞源访问警告
- 无新增编译错误
- `dotnet test` 退出码为 `0`
- 本轮新增 `3` 条回归用例，全部通过
- 启动验证通过，主窗口标题正常，`Responding = True`
- 测试运行残留已清理，未误删真实测试源码

---

## 给下一任的最后一句话

这份工作到现在，已经不适合再按“一个摘要文案点一轮”或“一个 history / notification 小入口一轮”去推进了。

summary flow 的对象层、history/notification 的结果态、task analysis sidebar、preview state，以及 `MainWindowShellCoordinator` / `PreviewRenderCoordinator` / `MainWindowHostController` 这一层，现在都已经收住了一大截。下一任请不要再回头补第一大块，而是直接把**第二大块**完整收完：

- 让 `ImagePreviewWindow` 从“刚有第一个 controller”推进到“至少 2 个以上真实 tool controller 的宿主”
- 把 `MainWindowViewModel` 开始从 partial 大宿主推进到更清晰的 feature state / coordinator 结构
- 压缩 `AppServices` 在 main-window / preview path 上最常走的运行期直连
- 再收紧一轮 `AppSettings` / `ConversionOptions` / `ConversionPreset` 的字段归属
- 改完还能稳定 `build + test + 启动`

到这一步为止，剩余工作已经不该再报“还要很多轮”；只剩最后这一大块，做完就该把这一阶段直接收官。
